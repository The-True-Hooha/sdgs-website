/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost','http://localhost:8888', 'https://sdgsfupre-ereyomi.000webhostapp.com/'],
  },
};

module.exports = nextConfig;
